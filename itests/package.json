{
  "name": "itests",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "cypress open",
    "itests": "wait-on http://backend:5000/api/v1/health && wait-on http://frontend:3000/ && cypress run --browser chrome",
    "fmt": "prettier --write \"cypress/**/*.js\""
  },
  "devDependencies": {
    "wait-on": "3.3.0",
    "cypress": "3.4.1",
    "prettier": "1.18.2"
  },
  "dependencies": {}
}
